// Only two major additions/changes

 // One, .includes() / The includes() method determines whether an array or a string includes a certain value among its entries, returning true or false as appropriate.

 "Helloooooo".includes("o"); // returns true

 const pets = ["cat", "dog", "bat"];

 pets.includes("dog"); //  returns true

// Two, exponential operator

const square = (x) => (x**2);
const cube = (y) => (y**3);
